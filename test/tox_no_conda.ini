[tox]
envlist = py27, py35, py36, py37

# Assumes this file is in the `./test` directory
toxworkdir = ../.tox_no_conda

# Assumes this file is in the `./test` directory
setupdir = ..                 

[testenv]
deps = pytest
extras = test

# Assumes this file is in the `./test/` directory
changedir = ..

commands = pytest -v {posargs}